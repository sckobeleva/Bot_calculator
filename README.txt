¬ файле archive.py перва€ верси€ калькул€тора, который выполн€ет 4 действи€ с натуральными числами.

¬ файле config.py служебна€ информаци€ (токен). ¬ дальнейшем там будут вспомогательные функции.

¬ файле bot.py актуальный телеграм-бот, который выполн€ет 4 действи€ с простыми и смешанными дроб€ми:
- запрашивает 1 аргумент (провер€ет, что он соответствует нужному формату и отличен от 0);
- предлагает выбрать действие через клавиатуру;
- запрашивает 2 аргумент (выполн€ет проверки);
- предлагает получить ответ через клавиатуру;
- выводит ответ.
„ерез eval() не стала реализовывать принципиально.

Ѕаги:
- неправильно определ€ет целую часть (и в целом ответ), если конечный результат меньше 0. —корее всего, дело в модуле fractions, потому что Fraction().__floor__() в консоли тоже считает неправильно. Ќадо разбиратьс€.

To do:
- залить работу с отрицательными числами (не протестить из-за блокирующего дефекта);
- повтор€ющийс€ код выделить в отдельные функции и переместить в config.py. ѕока не получаетс€ в параметре передавать именно нужный message.
- мелкие улучшени€.